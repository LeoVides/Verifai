<!DOCTYPE html>
<html>
  <head>
    <title>Verifai</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= user_signed_in? || !(controller_name == 'pages' && action_name == 'home') ? 'with-navbar' : 'without-navbar' %> ">
    <div class="d-flex">
      <!-- Show navbar only if user is logged in OR if not on homepage -->
      <% if user_signed_in? || !(controller_name == "pages" && action_name == "home") %>
        <%= render "shared/navbar" %>
      <% end %>


      <!-- Main Content -->
      <div class="main-content flex-grow-1">
        <%= render "shared/flashes" %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
