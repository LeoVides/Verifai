<!DOCTYPE html>
<html>
  <head>
    <title>Verifai</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= favicon_link_tag 'favicon.png', type: 'image/png' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

<body class="<%= user_signed_in? ? 'with-navbar' : 'without-navbar' %>">
  <div class="d-flex">
    <!-- Show navbar if the user is signed in OR if the user is NOT on home, login, or registration pages -->
    <% if user_signed_in? || !(controller_name == 'pages' && action_name == 'home') && !(controller_name == 'sessions' && action_name == 'new') && !(controller_name == 'registrations' && action_name == 'new') %>
      <%= render "shared/sidebar" %> <!-- Navbar component -->
    <% end %>

    <!-- Main Content -->
    <div class="wrapper">
      <div class="main-content flex-grow-1">
        <%= render "shared/flashes" %>
        <%= yield %> <!-- Content of the current page -->
      </div>
    </div>
  </div>
</body>

</html>
